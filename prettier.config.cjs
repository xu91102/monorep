module.exports = {
  // 一行最大字符数，超过会自动换行
  printWidth: 100,

  // 是否在语句末尾添加分号
  semi: true,

  // Vue 文件中是否缩进 script 和 style 标签
  vueIndentScriptAndStyle: true,

  // 使用单引号而不是双引号
  singleQuote: true,

  // 在对象或数组的最后一个元素后添加尾随逗号
  // 'all' - 尽可能添加尾随逗号
  trailingComma: 'all',

  // Markdown 文本是否换行
  // 'never' - 不换行
  proseWrap: 'never',

  // HTML 空白符敏感度
  // 'strict' - 严格模式，HTML 中的空白符会保留原样
  htmlWhitespaceSensitivity: 'strict',

  // 行尾符设置
  // 'auto' - 保持文件原有的行尾符（\n 或 \r\n）
  endOfLine: 'auto',
};
